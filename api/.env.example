# AgentHub API - Environment Configuration
# Copy to .env: cp .env.example .env

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
# local | dev | prod. In prod, API_KEY is required.
# ENVIRONMENT=local

# Display name (used in API responses, docs). Default: AgentHub
# APP_NAME=AgentHub

# -----------------------------------------------------------------------------
# CORS
# -----------------------------------------------------------------------------
# Comma-separated origins. Empty or "*" = allow all. In prod, set to frontend URLs.
# CORS_ALLOWED_ORIGINS=https://app.example.com,https://www.example.com

# -----------------------------------------------------------------------------
# API Authentication
# -----------------------------------------------------------------------------
# When set, all /api/v1/* and /metrics require X-API-Key header. /health is exempt.
# API_KEY=your-secret-key

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# SQLite (default, file-based, no server)
# DATABASE_URL=sqlite+aiosqlite:///./app.db

# PostgreSQL (async driver; postgres:// auto-converted to postgresql+asyncpg://)
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname
# DATABASE_URL=postgres://user:password@host:5432/dbname

# -----------------------------------------------------------------------------
# Redis
# -----------------------------------------------------------------------------
# Optional. When set: per-tenant rate limiting, document cache (300s TTL).
# REDIS_URL=redis://localhost:6379/0
# RATE_LIMIT_PER_MINUTE=120

# -----------------------------------------------------------------------------
# AI Audit Retention
# -----------------------------------------------------------------------------
# Purge ai_call_audit records older than N days. 0 = disabled.
# Run: python -m app.audit (e.g. via cron)
# AI_AUDIT_RETENTION_DAYS=90

# -----------------------------------------------------------------------------
# Search (Agent tool)
# -----------------------------------------------------------------------------
# duckduckgo: free, no API key
# tavily: better quality, requires TAVILY_API_KEY
# SEARCH_PROVIDER=duckduckgo
# TAVILY_API_KEY=tvly-xxx   # https://app.tavily.com/sign-in

# -----------------------------------------------------------------------------
# RAG Embeddings
# -----------------------------------------------------------------------------
# Currently only "mock" (deterministic hash-based). Future: openai, sentence-transformers.
# EMBEDDING_MODEL=mock
# EMBEDDING_DIMENSION=384

# -----------------------------------------------------------------------------
# LLM Backend
# -----------------------------------------------------------------------------
# Choose one: ollama or openai_compatible

# Ollama (local)
LLM_PROVIDER=ollama
LLM_BASE_URL=http://localhost:11434
LLM_MODEL=llama3.2

# OpenAI-compatible (OpenAI, vLLM, LocalAI, etc.) - uncomment and comment Ollama above
# LLM_PROVIDER=openai_compatible
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_MODEL=gpt-4
# LLM_API_KEY=sk-xxx

# LLM_TIMEOUT_SECONDS=60
# LLM_MAX_RETRIES=2

# -----------------------------------------------------------------------------
# Logging & Metrics
# -----------------------------------------------------------------------------
# LOG_LEVEL=INFO
# ENABLE_PROMETHEUS=true

# -----------------------------------------------------------------------------
# Tenant
# -----------------------------------------------------------------------------
# DEFAULT_TENANT_ID=default
# TENANT_HEADER_NAME=X-Tenant-ID
